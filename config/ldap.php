<?php

return [

    /*
    |--------------------------------------------------------------------------
    | إعدادات المصادقة عبر LDAP (Active Directory)
    |--------------------------------------------------------------------------
    |
    | نفس أسلوب المصادقة المستخدم في "نظام خدمة العملاء": يتم البحث عن
    | المستخدم محلياً بواسطة userID، ثم التحقق من كلمة المرور عبر محاولة
    | الربط (bind) بخوادم LDAP التالية بالتتابع حتى ينجح أحدها.
    |
    */

    'hosts' => array_values(array_filter(array_map(
        'trim',
        explode(',', env('LDAP_HOSTS', '192.168.161.100,192.168.161.131,192.168.161.201'))
    ))),

    'port' => (int) env('LDAP_PORT', 389),

    // بادئة الدومين المستخدمة في اسم الربط: "{prefix}\{userID}"
    'domain_prefix' => env('LDAP_DOMAIN_PREFIX', 'sfd'),

];
